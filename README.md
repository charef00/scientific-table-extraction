# scientific-table-extraction
A web-based application that automatically extracts tables from scientific PDF articles and converts them into structured datasets (Excel), with user-guided selection of relevant rows and column
